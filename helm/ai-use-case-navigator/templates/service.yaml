apiVersion: v1
kind: Service
metadata:
  name: {{ include "ai-use-case-navigator.fullname" . }}
  labels:
    {{- include "ai-use-case-navigator.labels" . | nindent 4 }}
  {{- with .Values.service.annotations }}
  annotations:
    {{- toYaml . | nindent 4 }}
  {{- end }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "ai-use-case-navigator.selectorLabels" . | nindent 4 }}
